<UserControl x:Class="Wpf.Views.StudentAnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Student Analytics"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">

            <TextBlock Text="Student:"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Margin="0,0,10,0"/>

            <ComboBox ItemsSource="{Binding Students}"
                      SelectedItem="{Binding SelectedStudent}"
                      DisplayMemberPath="FullName"
                      Width="250"
                      Height="30"
                      Margin="0,0,10,0"/>

            <Button Content="Load Results"
                    Command="{Binding LoadResultsCommand}"
                    Width="120"
                    Height="30"/>

            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       FontWeight="SemiBold"/>
        </StackPanel>

        <GroupBox Grid.Row="2"
                  Header="Student Exams"
                  Margin="0,0,0,15">

            <DataGrid ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedResult}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Result ID" Binding="{Binding Id}" Width="90"/>
                    <DataGridTextColumn Header="Test ID" Binding="{Binding MathTestId}" Width="100"/>
                    <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="100"/>
                    <DataGridTextColumn Header="Total Questions" Binding="{Binding TotalQuestions}" Width="130"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                    <DataGridTextColumn Header="Completed At" Binding="{Binding CompletedAt}" Width="180"/>
                </DataGrid.Columns>

            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="3"
                  Header="Task Details">

            <DataGrid ItemsSource="{Binding SelectedResult.Tasks}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Task ID" Binding="{Binding TaskId}" Width="90"/>
                    <DataGridTextColumn Header="Expression" Binding="{Binding Expression}" Width="*"/>
                    <DataGridTextColumn Header="Expected Result" Binding="{Binding ExpectedResult}" Width="140"/>
                    <DataGridTextColumn Header="Student Result" Binding="{Binding StudentResult}" Width="140"/>
                    <DataGridCheckBoxColumn Header="Correct" Binding="{Binding IsCorrect}" Width="100"/>
                </DataGrid.Columns>

            </DataGrid>
        </GroupBox>

    </Grid>
</UserControl>